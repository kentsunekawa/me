node_modules
dist
*.local
*.tsbuildinfo

# content/private は data/resources（private リポ me-resources）への symlink。
# 実データ（住所・写真など）は me-resources 側で管理し、公開リポ（me）には出さない。
# symlink 自体もコミットしない（クローン時に作り直す。README 参照）。
content/private
